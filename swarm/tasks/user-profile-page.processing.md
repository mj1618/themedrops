# User Profile Page

## Status: Processing

Claimed by coder agent. The app/ directory was missing (previous work not committed).
Rebuilding the full Next.js + Convex app from scratch, including the user profile page.

## Original Goal

Create `/u/[username]` — the author profile page that theme cards and the theme detail page already link to. The backend is fully implemented (`getUserByUsername`, `listByUser`, `getMe`). This is currently a broken link in the app.
